#include <stdio.h>
#include <math.h>

int main()
{
    int a = 5, b = 6;

    scanf("%d %d", &a, &b);

    printf(" a + b = %d\n", a + b);
    printf(" a - b = %d\n", a - b);
    printf(" a * b = %d\n", a * b);
    printf(" a / b = %d\n", a / b);

    return 0;
}